# PyTorch — instale a versão compatível com ROCm ou CUDA manualmente.
# ROCm (cluster AMD): pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
# CUDA:               pip install torch
torch>=2.1.0

# HuggingFace stack
transformers>=4.45.0
accelerate>=0.26.0
datasets>=2.18.0

# Fine-tuning eficiente
peft>=0.10.0         # LoRA / DoRA (use_dora=True)
trl>=0.8.0           # SFTTrainer
bitsandbytes>=0.43.0 # QLoRA / NF4 — suporte ROCm a partir de 0.43

# Avaliação NER (H1)
seqeval>=1.2.2

# Avaliação de capacidades gerais (H2)
lm-eval>=0.4.3       # lm-evaluation-harness (EleutherAI)

# Utilitários
numpy>=1.24.0
scikit-learn>=1.3.0
tqdm>=4.66.0
