# PyTorch — install the ROCm- or CUDA-compatible build manually.
# ROCm (AMD cluster): pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
# CUDA:               pip install torch
torch>=2.1.0

# HuggingFace stack
transformers>=4.45.0
accelerate>=0.26.0
datasets>=2.18.0

# Efficient fine-tuning
peft>=0.10.0         # LoRA / DoRA (use_dora=True)
trl>=0.8.0           # SFTTrainer
bitsandbytes>=0.43.0 # QLoRA / NF4 — ROCm support from 0.43

# NER evaluation (H1) — pinned: it defines the IOB2/strict parsing of the reported metrics
seqeval==1.2.2

# General-ability evaluation (H2) — pinned: the version changes the harness tasks/prompts
lm-eval==0.4.9       # lm-evaluation-harness (EleutherAI)

# Utilities
numpy>=1.24.0
scikit-learn>=1.3.0
tqdm>=4.66.0
